#include<stdio.h>
int main(){
    int a =10;
    int result;

    // result=+a;
    // result =-a;
    // pre-increment operator
    // result= ++a;
    // postincrement method will increment the value only after it has once been printed....
    // result=a++;
    result =--a;
    printf("the result of the result is %d",result);
    return 0;
}